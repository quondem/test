"use strict";

var signUp = document.querySelector(".sign-up__submit"),
  firstName = document.querySelector(".first input"),
  lastName = document.querySelector(".last input"),
  nation = document.querySelector(".nation input"),
  email = document.querySelector(".email input"),
  password = document.querySelector(".password input"),
  confirm = document.querySelector(".confirm input"),
  inputs = [firstName, lastName, nation, email, password, confirm],
  info = [firstName, lastName, nation],
  content = document.querySelector(".sign-up__sign"),
  message = document.querySelector(".message"),
  patterns = {
    email: /\w+@\w+\.\w+$/g,
    password: /(?=.*[0-9])(?=.*[a-z])(?=.*[A-Z])[0-9a-zA-Z]{8,}$/g
  };
function checkNull() {
  for (var _i = 0, _inputs = inputs; _i < _inputs.length; _i++) {
    var e = _inputs[_i];
    if ("" == e.value) return e.classList.add("error"), !1;
  }
  return !0;
}
email.addEventListener("input", function (e) {
  this.value.match(patterns.email) ? this.classList.remove("error") : this.classList.add("error");
}), password.addEventListener("input", function (e) {
  this.value.match(patterns.password) ? (this.classList.remove("error"), this.nextElementSibling.classList.add("disabled")) : (this.classList.add("error"), this.nextElementSibling.classList.remove("disabled"));
}), confirm.addEventListener("input", function (e) {
  this.value == password.value ? this.classList.remove("error") : this.classList.add("error");
});
for (var _i2 = 0, _info = info; _i2 < _info.length; _i2++) {
  var e = _info[_i2];
  e.addEventListener("input", function () {
    "" == this.value ? this.classList.add("error") : this.classList.remove("error");
  });
}
var clicked = !1;
signUp.addEventListener("click", function (e) {
  var _this = this;
  e.preventDefault(), clicked || (checkNull() && !document.querySelector(".error") ? (content.classList.add("disabled"), message.classList.remove("disabled")) : (this.classList.add("animate__shakeX"), setTimeout(function () {
    _this.classList.remove("animate__shakeX"), clicked = !1;
  }, 1500))), clicked = !0;
});
//# sourceMappingURL=main.min.js.map
